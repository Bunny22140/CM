Create An EC2 Instance
Security Group t3 micro ams linux , key Pair
ssh anywhere , All Traffic Anywhere and MYSQL Anywhere

in aws console

sudo dnf install mariadb105-server -y
sudo systemctl start mariadb
sudo systemctl enable mariadb
sudo mysql

CREATE DATABASE studentdb;
USE studentdb;

CREATE TABLE students (
  id INT AUTO_INCREMENT PRIMARY KEY,
  name VARCHAR(50)
);

INSERT INTO students (name) VALUES ('Andrew');
select * from students;
exit;

rds database
studentdb-instance or rds

mysql engine
full configuration
free tier

studentdb-instance

admin 
pass 12345678

vpc and sg same as ec2 ,  no public access

copy rds endpoint

in ec2 console

mysql -h studentdb-instance.cx6ko6ciy1i3.ap-south-1.rds.amazonaws.com -u admin -p
CREATE DATABASE studentdb;
exit;

sudo mysqldump studentdb > studentdb_backup.sql

mysql -h studentdb-instance.cx6ko6ciy1i3.ap-south-1.rds.amazonaws.com -u admin -p studentdb < studentdb_backup.sql

mysql -h studentdb-instance.cx6ko6ciy1i3.ap-south-1.rds.amazonaws.com -u admin -p
USE studentdb;
SHOW TABLES;
SELECT * FROM students;
